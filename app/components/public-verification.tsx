"use client";import{useEffect,useState}from"react";export function PublicVerification({id}:{id:string}){const[data,setData]=useState<Record<string,unknown>|null>(null);const[error,setError]=useState<string|null>(null);useEffect(()=>{let active=true;fetch(`/api/credentials/${encodeURIComponent(id)}/verify`,{cache:"no-store"}).then(async(response)=>{const payload=await response.json()as{credential?:Record<string,unknown>;error?:string};if(!active)return;if(response.ok&&payload.credential)setData(payload.credential);else setError(payload.error??"Không thể kiểm tra credential.");});return()=>{active=false;};},[id]);if(error)return <div className="verification-result denied"><strong>NOT VERIFIED</strong><p>{error}</p></div>;if(!data)return <div className="verification-result"><strong>CHECKING DEVNET…</strong></div>;const chain=data.chainStatus as{valid:boolean;reason:string};return <div className={`verification-result ${chain.valid?"verified":"denied"}`}><span>{chain.valid?"✓ LIVE DEVNET PROOF":"× ACCESS DENIED"}</span><h2>{String(data.challenge_title)}</h2><div className="verify-score"><strong>{String(data.score)}</strong><small>/100</small></div><dl><div><dt>ISSUER</dt><dd>{String(data.issuer_name)}</dd></div><div><dt>SUBJECT</dt><dd>{String(data.student_wallet)}</dd></div><div><dt>STATUS</dt><dd>{String(data.status)} · {chain.reason}</dd></div><div><dt>EXPIRES</dt><dd>{String(data.expires_at)}</dd></div></dl><a className="button button-dark" href={String(data.attestationExplorer)} target="_blank" rel="noreferrer">View attestation on Explorer ↗</a></div>;}
