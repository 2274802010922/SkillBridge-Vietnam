import{AppHeader,AppSidebar}from"../../components/app-header";import{PassportWorkspace}from"../../components/passport-workspace";import{requirePageSession}from"../../../lib/page-session";export const dynamic="force-dynamic";export default async function PassportPage(){const{user}=await requirePageSession("/app/passport");return <main className="product-app"><AppHeader walletAddress={user.walletAddress}/><div className="app-layout page-shell"><AppSidebar active="passport"/><PassportWorkspace/></div></main>;}
