import {env}from"@/backend/config/runtime-env";import{OpportunitiesWorkspace}from"../../../frontend/features/opportunities/opportunities-workspace";import{requirePageSession}from"../../../backend/auth/page-session";export const dynamic="force-dynamic";export default async function OpportunitiesPage(){const{memberships}=await requirePageSession("/app/opportunities");const universities=await env.DB.prepare("SELECT id,name FROM organizations WHERE kind='university' ORDER BY name").all();return <OpportunitiesWorkspace memberships={memberships as never[]} universities={universities.results as never[]}/>;}
